---
title: "Fuel Wallet"
---

## Overview

The Fuel wallet provider supports Fuel Network wallets and ecosystem.

## Installation

<CodeGroup>
```typescript yarn theme={"system"}
yarn add @layerswap/wallet-fuel
```
```typescript pnpm theme={"system"}
pnpm add @layerswap/wallet-fuel
```
```typescript npm theme={"system"}
npm install @layerswap/wallet-fuel
```
</CodeGroup>

## Basic Usage

The Fuel provider requires no configuration:

```typescript
import { LayerswapProvider, Swap } from "@layerswap/widget"
import { createFuelProvider } from "@layerswap/wallet-fuel"
import "@layerswap/widget/index.css"

export const App = () => {
  const fuelProvider = createFuelProvider()

  return (
    <LayerswapProvider
      walletProviders={[fuelProvider]}
    >
      <Swap />
    </LayerswapProvider>
  )
}
```