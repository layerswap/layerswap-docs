# Layerswap single-page docs prototype

An isolated, dependency-free prototype for evaluating a compact human-facing Layerswap documentation model.

Run `npm run dev` from this directory and open `http://localhost:4173`.

The prototype intentionally links back to the current exhaustive documentation. It does not replace, delete, or restructure the Mintlify documentation in the repository.
